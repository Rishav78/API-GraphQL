const message = require('./messageServices');
const verify = require('./verifyServices');
const token = require('./tokenServices');
const file = require('./fileServices');

module.exports = {
    message,
    verify,
    token,
    file
}